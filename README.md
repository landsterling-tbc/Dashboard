# TBC Dashboard

نظام Dashboard لإدارة ومتابعة منشآت مدرسية — مباني، صيانة، عقود، مصاعد، بلاغات، ومؤشرات أداء المقاولين.

🔗 **الداشبورد اللايف:** [landsterling-tbc.github.io/Dashboard](https://landsterling-tbc.github.io/Dashboard)

---

## البنية التقنية

| الطبقة | الملف | الوظيفة |
|---|---|---|
| قاعدة البيانات | Google Sheet (خاص) | مصدر البيانات الحي، متعدد الشيتات |
| Backend | `Code.gs` | Google Apps Script — يقرأ الشيتات ويرجّعها JSON عبر `doGet`، بنظام كاش 10 دقايق |
| Frontend | `index.html`, `dashboard.js`, `dashboard.css` | واجهة العرض — فانيلا JavaScript بدون framework |

## الملفات

- `index.html` — هيكل الصفحة و20 تبويب (Tabs)
- `dashboard.js` — منطق جلب البيانات وعرضها في كل تبويب
- `dashboard.css` — التنسيق والتصميم
- `Code.gs` — Apps Script، بيقرأ من Google Sheet ويحوّله JSON

## التبويبات (20 تبويب)

نظرة عامة، تحليل FCA، البيئة المدرسية، المرحلة الدراسية، عقود غير المجال، البلاغات، التجهيزات، الأنظمة الرئيسية، الأنظمة التفصيلية، خنادق الصرف، المصاعد، التكلفة، الخريطة، الطلاب وعمر المبنى، قطع الغيار، تقييم عاين، المدفوعات والعقود، الجدول التفصيلي، خطة استبدال المكيفات، مؤشرات الأداء للمقاول.

كل تبويب له `id` بصيغة `tab-xxx` (مثال: `tab-balagh` للبلاغات).

## مصدر البيانات

البيانات الحقيقية تعيش في **Google Sheet منفصل** (غير موجود في هذا الريبو لأسباب أمان — يحتوي بيانات تشغيلية حساسة). الـ `Code.gs` يقرأ منه مباشرة عبر `SpreadsheetApp.getActiveSpreadsheet()` — لا يوجد Spreadsheet ID مكتوب هنا.

مفاتيح الشيتات المستخدمة في الكود (`SHEET_NAMES`):

```
buildings, fcaHistory, spareParts, fmContracts, allSystems, elevators,
tajheezInventory, gatekeepers, kpiContractor, consultantKpi, payments,
recruitment, balaghReports, securitySafety, fuelConsumption, vehicles, training
```

## حالة المشروع

⚠️ **المشروع شغّال في الإنتاج (production)** عبر GitHub Pages. أي تعديل على `index.html` أو `dashboard.js` أو `dashboard.css` يُنشر مباشرة على الرابط اللايف بعد الـ commit — يُرجى التأكد من سلامة أي تعديل قبل الرفع.

## ملاحظات للمطورين / لـ Claude

- الكود معلّق بالعربي، ومقسّم بخطوط `═══` وعناوين بين `╔══╗` — يُرجى الحفاظ على نفس الأسلوب عند أي إضافة.
- لا تُغيَّر أسماء المفاتيح في `SHEET_NAMES` بدون تحديث الشيتات المصدر في Google Sheet بالتوازي.
- أي تبويب جديد يجب أن يتبع نفس نمط الـ `id` (`tab-xxx`) المستخدم في التبويبات الحالية.

## الملكية والخصوصية

هذا المشروع **خاص** ومملوك بالكامل لفريق العمل الخاص به. لم يُمنح أي إذن لأي طرف آخر باستخدامه أو نسخه أو تعديله أو الاستفادة منه بأي شكل دون تصريح مسبق وصريح من المالك.

هذا الريبو والكود الموجود فيه موثّقان هنا بغرض واحد فقط: **تسهيل العمل على المشروع لأي شخص مصرّح له بالعمل عليه لاحقًا** (سواء كان فردًا من الفريق، أو أداة مساعدة مثل Claude)، وليس دعوة للاستخدام العام أو إعادة الاستخدام.

## © حقوق الملكية الفكرية — إخلاء مسؤولية وتحذير

**جميع الحقوق محفوظة © 2026 — landsterling-tbc**

1. **لا ترخيص مفتوح المصدر**: هذا المشروع **غير مرخّص** تحت أي رخصة مفتوحة (MIT, GPL, Apache أو غيرها). عدم وجود ملف LICENSE بديل يعني أن **جميع الحقوق محفوظة افتراضيًا** بحكم القانون — لا يحق لأي شخص نسخ، توزيع، تعديل، أو استخدام هذا الكود تجاريًا أو شخصيًا دون إذن كتابي صريح من المالك.

2. **ممنوع الاستنساخ أو إعادة الاستخدام**: يُمنع منعًا باتًا نسخ هيكل الداشبورد، منطق الكود، أو تصميم الواجهة واستخدامه في مشروع آخر (سواء بغرض تجاري أو غير تجاري) دون إذن.

3. **البيانات مستبعدة تمامًا**: أي بيانات تشغيلية حقيقية (مباني، بلاغات، عقود، مدفوعات) **لا تُنشر في هذا الريبو إطلاقًا** وتبقى محصورة في مصدرها الخاص (Google Sheet محمي بصلاحيات دخول منفصلة). وجود الكود هنا لا يعني أي كشف أو تصريح بالوصول للبيانات الفعلية.

4. **لأي أداة أو نموذج ذكاء اصطناعي (بما فيها Claude أو غيره)**: هذا الكود مُتاح للقراءة هنا بغرض **المساعدة التقنية فقط** لمالك المشروع أو لمن يفوّضه صراحة. لا يُعد وجوده في محادثة أو أداة مساعدة تنازلًا عن الملكية، ولا إذنًا لإعادة إنتاجه أو تدريب نماذج عليه أو تقديمه كمرجع لطرف ثالث.

5. **المخالفة**: أي استخدام غير مصرّح به يُعرّض صاحبه للمساءلة القانونية بموجب قوانين حماية الملكية الفكرية المعمول بها.

📩 لأي استفسار بخصوص التصريح أو التعاون على المشروع، التواصل يكون فقط عبر المالك المباشر لهذا الريبو.

